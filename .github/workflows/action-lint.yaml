name: Lint Actions

on:
  pull_request:
    paths:
      - .github/workflows/**
  push:
    branches: [master]
    paths:
      - .github/workflows/**

jobs:
  lint:
    uses: growombud/gha-shared/.github/workflows/lint-actions.yml@main
